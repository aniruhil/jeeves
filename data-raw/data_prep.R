library(gutenbergr)

mymanjeeves <- gutenberg_download(8164, meta_fields = "title")
devtools::use_data(mymanjeeves, overwrite = TRUE)

righthojeeves <- gutenberg_download(10554, meta_fields = "title")
devtools::use_data(righthojeeves, overwrite = TRUE)

themanwithtwoleftfeet <- gutenberg_download(7471, meta_fields = "title")
devtools::use_data(themanwithtwoleftfeet, overwrite = TRUE)

somethingnew <- gutenberg_download(2042, meta_fields = "title")
devtools::use_data(somethingnew, overwrite = TRUE)

themanupstairs <- gutenberg_download(6768, meta_fields = "title")
devtools::use_data(themanupstairs, overwrite = TRUE)

psmithinthecity <- gutenberg_download(6753, meta_fields = "title")
devtools::use_data(psmithinthecity, overwrite = TRUE)

thegirlontheboat <- gutenberg_download(20717, meta_fields = "title")
devtools::use_data(thegirlontheboat, overwrite = TRUE)

adamselindistress <- gutenberg_download(2233, meta_fields = "title")
devtools::use_data(adamselindistress, overwrite = TRUE)

theadventuresofsally <- gutenberg_download(7464, meta_fields = "title")
devtools::use_data(theadventuresofsally, overwrite = TRUE)

piccadillyjim <- gutenberg_download(2005, meta_fields = "title")
devtools::use_data(piccadillyjim, overwrite = TRUE)

awodehousemiscellany <- gutenberg_download(8190, meta_fields = "title")
devtools::use_data(awodehousemiscellany, overwrite = TRUE)

psmithjournalist <- gutenberg_download(2607, meta_fields = "title")
devtools::use_data(psmithjournalist, overwrite = TRUE)

theclickingofcuthbert <- gutenberg_download(7028, meta_fields = "title")
devtools::use_data(theclickingofcuthbert, overwrite = TRUE)

uneasymoney <- gutenberg_download(6684, meta_fields = "title")
devtools::use_data(uneasymoney, overwrite = TRUE)

mikeandpsmith <- gutenberg_download(10586, meta_fields = "title")
devtools::use_data(mikeandpsmith, overwrite = TRUE)

deathattheexcelsiorandotherstories <- gutenberg_download(8176, meta_fields = "title")
devtools::use_data(deathattheexcelsiorandotherstories, overwrite = TRUE)

indiscretionsofarchie <- gutenberg_download(3756, meta_fields = "title")
devtools::use_data(indiscretionsofarchie, overwrite = TRUE)

loveamongthechickens <- gutenberg_download(3829, meta_fields = "title")
devtools::use_data(loveamongthechickens, overwrite = TRUE)

mike <- gutenberg_download(7423, meta_fields = "title")
devtools::use_data(mike, overwrite = TRUE)

thepolitenessofprincesandotherstories <- gutenberg_download(8178, meta_fields = "title")
devtools::use_data(thepolitenessofprincesandotherstories, overwrite = TRUE)

amanofmeans <- gutenberg_download(8713, meta_fields = "title")
devtools::use_data(amanofmeans, overwrite = TRUE)

theintrusionofjimmy <- gutenberg_download(4075, meta_fields = "title")
devtools::use_data(theintrusionofjimmy, overwrite = TRUE)

jillthereckless <- gutenberg_download(20533, meta_fields = "title")
devtools::use_data(jillthereckless, overwrite = TRUE)

theswooporhowclarencesavedengland <- gutenberg_download(7050, meta_fields = "title")
devtools::use_data(theswooporhowclarencesavedengland, overwrite = TRUE)

threemenandamaid <- gutenberg_download(6836, meta_fields = "title")
devtools::use_data(threemenandamaid, overwrite = TRUE)

thepothunters <- gutenberg_download(6984, meta_fields = "title")
devtools::use_data(thepothunters, overwrite = TRUE)

thewhitefeather <- gutenberg_download(6927, meta_fields = "title")
devtools::use_data(thewhitefeather, overwrite = TRUE)

aprefectsuncle <- gutenberg_download(6985, meta_fields = "title")
devtools::use_data(aprefectsuncle, overwrite = TRUE)

theheadofkays <- gutenberg_download(6877, meta_fields = "title")
devtools::use_data(theheadofkays, overwrite = TRUE)

talesofstaustins <- gutenberg_download(6980, meta_fields = "title")
devtools::use_data(talesofstaustins, overwrite = TRUE)

thegoldbat <- gutenberg_download(6879, meta_fields = "title")
devtools::use_data(thegoldbat, overwrite = TRUE)

williamtelltoldagain <- gutenberg_download(7298, meta_fields = "title")
devtools::use_data(williamtelltoldagain, overwrite = TRUE)

theprinceandbetty <- gutenberg_download(6955, meta_fields = "title")
devtools::use_data(theprinceandbetty, overwrite = TRUE)

thelittlenugget <- gutenberg_download(6683, meta_fields = "title")
devtools::use_data(thelittlenugget, overwrite = TRUE)

thegemcollector <- gutenberg_download(8931, meta_fields = "title")
devtools::use_data(thegemcollector, overwrite = TRUE)

thecomingofbill  <- gutenberg_download(6880, meta_fields = "title")
devtools::use_data(thecomingofbill, overwrite = TRUE)

thelittlewarrior <- gutenberg_download(6837, meta_fields = "title")
devtools::use_data(thelittlewarrior, overwrite = TRUE)

notgeorgewashington <- gutenberg_download(7230, meta_fields = "title")
devtools::use_data(notgeorgewashington, overwrite = TRUE)


